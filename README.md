# friendly-parakeet

This project demonstrates a lightweight compliance-friendly foundation with:

- Secure secret loading via `config/secrets.py`
- Audited resource access and outbound email logging in `app/audit.py`
- Automated personal data retention and purge workflow in `app/purge.py`

Run tests with:

```bash
python -m pytest -q
```
